<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://unpkg.com/prismjs@1.20.0/themes/prism-okaidia.css" rel="stylesheet">
    <title>Snipppet explorer</title>
</head>
<body class="antialiased bg-gray-900">
    <div class="container mx-auto py-10">
        {% for snippet in allSnippets %}
            <div class="mt-10">
                <code class="bg-gray-100 border border block px-4 py-1 rounded">{{ snippet.name }}</code>

                {% for lang, code in snippet.data %}
                    <div class="mt-2 relative">
                    {% highlight lang %}{{ code | safe }}{% endhighlight %}
                        <div class="absolute top-0 right-0 p-4 text-gray-100">{{ lang }}</div>
                    </div>
                {% endfor %}
            </div>
        {% endfor %}
    </div>
</body>
</html>